# 하위헌스-프레넬 장애물 모형 (대조군) — 인수인계

- 작성: 2026-08-05 · Phase 1 (하위헌스 앱) 완료 시점
- 설계 문서: 바깥 저장소 `docs/superpowers/specs/2026-08-05-huygens-obstacle-design.md`
  (커밋 `bd0f660` — Phase 1 실측 반영본. 복사 시점 기준은 `5f5c564`)
- 브랜치: `feature/huygens-obstacle` (기본 브랜치는 `main`)
- 원본 복사 시점 커밋: `0cf9519`

> **Phase 2(`compare/` 비교 페이지)는 아직 만들지 않았습니다.**

---

## 1. 하위헌스-장애물 모형의 정식화

2차원 스칼라 문제. 화면 `x=0` 평면에 도선 배열이 있고, 도선 `n`은 구간 `|y − y_n| ≤ a`를
완전히 가로막는 **불투명 띠**다. 도선에 유도전류가 흐르지 않으며, 열린 틈에서만 2차 구면파가
나온다. 평면파 `E_inc = e^{ikx}`가 왼→오른으로 입사한다. 시간 규약은 원본 앱과 같은
`e^{i(kx−ωt)}`, 렌더 값은 `val = (fr·cosφ + fi·sinφ)·A`.

**바비네 뒤집기.** 구멍(열린 부분)을 직접 적분하지 않는다. 열린 부분 전체는 무한 배경을
포함해 적분 절단 오차가 생기지만, **막힌 부분만 적분하면 유한하고 수학적으로 완전히 동일**하다.

```
x > 0 :  E_total(x,y) = e^{ikx} − (ik/2)·Σ_n ∫_{y_n−a}^{y_n+a} H₁⁽¹⁾(k r)·(x/r) dy'
         r = sqrt(x² + (y − y')²)

x < 0 :  E_total(x,y) = e^{ikx}        (반사파 없음 — 이 모형의 본질적 한계)
```

**`HUYGENS_SIGN = −1` 의 유도.** 토글이 아니라 고정 상수다.

```
2차원 각스펙트럼 임펄스 응답  K = −2·∂G/∂x,  G = (i/4)H₀⁽¹⁾(kr)
(H₀)' = −H₁  ⇒  K = +(ik/2)·H₁⁽¹⁾(kr)·(x/r)
열린 부분 투과함수 τ = 1 − B (B = 막힘 지시함수) 를 대입하면
전 평면 항이 e^{ikx} 를 주고, 막힌 항에 − 가 붙는다.
```

원본 요구서(`superpowers-prompt-huygens-grid.md`) §2-2의 `+`는 오류였고, 같은 문서 §2-3의
해석식 `t_m = δ_{m0} − (2a/d)·sinc(α_m a)`와 자기모순이었다. 수치 대조로 확인했다
(해석해 대비 상대오차 `3.4e-5` 단일차수 / `1.0e-5` 다차수).

**경사인자 `x/r` 도 토글이 아니다.** 키르히호프 경사인자 `(1+cosθ)/2`로 바꾸면 해석해 `t_m`
대조(검증의 핵심 축)가 성립하지 않는다. **RS-1종 경사인자가 해석해 대조의 전제**다.

---

## 2. 이 모형이 표현하지 못하는 것

논문에서 이 모형은 **대조군**이다. 아래 셋은 결함이 아니라 이 모형이 무엇을 빠뜨렸는지를
보여 주는 지점이므로 그대로 드러내야 한다.

| 못 하는 것 | 결과 |
|---|---|
| **반사** | `x < 0`에서 `E_total ≡ E_inc`. 차이장이 정확히 0 (부동소수점 0, 근사 아님) |
| **에너지 보존** | `R = 0` 이므로 `T + R ≠ 1`. 정보란에 경고로 표시한다 |
| **편광 의존성** | 스칼라 이론이라 `E∥`·`E⊥` 결과가 **비트 단위로 동일**. 편광 버튼은 비활성 |

**논문에서의 역할.** 하위헌스-장애물 모형의 투과율은 파장에 거의 무관한 상수 `(1−2a/d)²`로
고정되어, 파장이 아무리 길어져도 차폐가 생기지 않는다. 즉 "패러데이 차폐는 기하학적 가림이
아니라 유도전류의 재방사 때문"이라는 논지의 **정량적 반례**다.
실측: `d=10 mm`에서 `|T(λ=15 cm) − T(λ=25 cm)| = 0` (부동소수점 0).

---

## 3. Tab 0 = 해석급수 / Tab 1 = 수치 적분 의 이원 구조

| | Tab 0 (무한 배열) | Tab 1 (유한 배열) |
|---|---|---|
| 방법 | 해석급수 (Floquet 차수 전개) | RS-1종 수치 적분 |
| 격자 폭 | `GRID_W_INF = 360` | `GRID_W_FINITE = 200` |
| `N` | 표시 전용 (계산에 안 들어감) | 계산에 들어감 |

```
Tab 0:  E_total(x>0) = Σ_m t_m·e^{i(α_m y + κ_m x)}
        α_m = 2πm/d,  κ_m = sqrt(k²−α_m²) (전파) 또는 i·g_m (소멸)
        t_m = δ_{m0} − (2a/d)·sinc(α_m a)
```

**왜 Tab 0을 수치 적분으로 그리지 않는가.** 화면에 보이는 유한개 도선만 합산하면 가장자리
회절이 화면에 들어와, **정보란의 해석식 `T`와 그림이 서로 다른 물리**가 된다. 해석급수로
그리면 그림과 `T`가 같은 물리를 말한다. 덤으로 **두 앱의 탭 의미가 일대일 대응**한다 —
Tab 0 = 해석해/무한(원본은 Floquet, 대조군은 하위헌스 급수), Tab 1 = 수치/유한(원본은 MoM,
대조군은 RS 적분).

**구현 메모 — `±m` 쌍 묶기.** `t_m`과 `κ_m`은 `m`의 우함수이고 `α_{−m} = −α_m`이므로

```
E_diff = (t₀ − 1)·e^{ikx} + Σ_{m=1}^{M} 2·t_m·cos(α_m y)·e^{iκ_m x}
```

로 묶인다. 계산량이 절반이고 `y → −y` 대칭이 부동소수점 수준에서 정확해진다. 행방향 인자
`2 t_m cos(α_m y)`와 열방향 인자 `e^{iκ_m x}`로 분리해 외적으로 누산한다.

> **⚠ 소멸차수 조기종료는 반드시 포락선으로 판정할 것.**
> `t_m`은 `α_m a = π`인 차수에서 **정확히 0**이 된다(`d=30 mm`·`a=1 mm`면 `m=15`).
> `|t_m|·e^{−g_m x} < ε` 로 `break` 하면 그 지점에서 **아직 살아 있는 뒤쪽 소멸파를 통째로
> 버린다.** 근거리 `x=1.19 mm`에서 `6.5e-4` 짜리 **순실수** 오차로 나타났다(소멸차수 기여가
> 실수라서 허부는 멀쩡했다). 종료는 상계 `2a/d`로 판정하고 개별 항은 건너뛰기만 한다.
> 수정 후 `RS(N=6401, nS=512)`와의 편차 `6.517e-4 → 3.34e-7`.

> **⚠ `EVANESCENT_EPS` 가 `HUYGENS_M` 보다 먼저 급수를 자르면 안 된다.**
> `EPS = 1e-7` 이던 시절, 최근접 열에서 λ=12.2 cm·d=10 mm 는 `m = 28`,
> λ=1 cm·d=60 mm 는 `m = 166` 에서 잘려 **`M = 200` 에 닿지도 못했다.** 실오차가
> `3.3e-7 / 2.8e-7 / 1.2e-6` 으로, `M` 근거표가 약속한 `1e-10` 수준보다 3~10⁴배 컸다.
> `EPS` 는 **항별** 임계인데 잘린 항이 수십 개면 누적 오차가 임계 자체보다 커지기 때문이다.
> `EPS = 1e-10` 으로 조여 절단을 `m = 42 / 249 / 122` 로 밀어냈다.
> **`EPS` 나 `M` 을 건드리면 아래 §5 의 "조기종료 절단 m" 열을 반드시 다시 잴 것.**

---

## 4. 참고 수치

### 무한 배열 (해석식)

| λ | d | a | T (실제, Floquet) | T (하위헌스) |
|---|---|---|---|---|
| 12.2 cm | 10 mm | 0.5 mm | 3.5 % | 81.00 % |
| 25 cm | 10 mm | 0.5 mm | 0.8 % | 81.00 % |
| 5 cm | 10 mm | 0.5 mm | 18.2 % | 81.00 % |
| 2 cm | 10 mm | 0.5 mm | 64.2 % | 81.00 % |
| 12.2 cm | 10 mm | 3 mm | 1.6 % | 16.00 % |
| 12.2 cm | 30 mm | 1 mm | 38.2 % | 87.11 % |
| 1 cm | 30 mm | 1 mm | 83.3 % | 88.56 % (전파차수 5개) |
| 12.2 cm | 3 mm | 0.5 mm | 0.0 % | 44.44 % |

`1 cm / 30 mm` 행의 "전파차수 5개"는 `m=±3`이 정확히 `κ=0`이라 제외되기 때문이다.
`T(실제)` 열은 `node shared/floquet-ref.test.js`가 소수점 1자리까지 대조한다(9건 통과).
`T(하위헌스)` 열은 `verify.js` 항목 7이 `±0.05 %p`로 대조한다.

> **⚠ `1 cm / 30 mm` 행 — 두 모형이 `κ = 0` 을 다르게 겪는다.**
> - **하위헌스는 안전하다.** `T = Σ (κ_m/k)|t_m|²` 에서 `m=±3` 의 가중치 `κ/k` 가 0 이라
>   넣든 빼든 값이 같다. `88.6 %` 는 임계값에 의존하지 않는다.
> - **Floquet 은 다르다.** `floquetZ` 급수에 `1/κ_m` 이 있어 `κ ≈ 0` 에서 발산한다.
>   `83.3 %` 는 원본의 가드 상수에 **의존하는 값**이다. 그래서
>   `shared/floquet-ref.js` 는 원본과 **글자 그대로 같은 가드**를 쓴다 (대조 확인 완료):
>   `floquetZ` 의 `mag2 < 1e-30`, `T` 합산의 `thr = 1e-9·k²`, `FLOQUET_M = 50`,
>   `A_RATIO_MAX = 0.30`. 이것이 보장되지 않으면 "복사본 T 가 원본 `#infoBox` 와 소수점
>   1자리까지 일치" 검증이 의미를 잃는다. **원본 `script.js` 가 바뀌면 이 넷을 먼저 대조할 것.**

> **`T (하위헌스)` 열은 소수 둘째 자리까지 적는다.** 한 자리로 적으면 항목 7 의 편차가
> 실제 오차가 아니라 **표의 반올림**에서 생긴다 — `44.4 %`로 적으면 정확값 `44.4444 %`와의
> 차이 `0.0444 %p`가 기준 `0.05 %p`를 거의 다 먹어 버렸다. 둘째 자리로 적으니 최대 편차가
> **`0.0044 %p`**로 떨어졌다. 여기서 실패한다면 `t₀` 계산이 아니라 `aEffMm`
> 클램프(`min(a, 0.30·d)`)를 빠뜨린 것이다.

### 유한 배열 (λ=12.2 cm, d=10 mm, a=0.5 mm)

| 항목 | 값 |
|---|---|
| `T_inf_grid` | 3.5 % |
| `T_fin_grid (N=30)` | 3.3 % |
| `T_fin_grid (N=60)` | 3.0 % |
| `T_inf_huygens` | 81.0 % |
| `T_fin_huygens (N=30)` | 81.66 % |

---

## 5. 상수·토글 목록

| 이름 | 값 | 성격 | 설명 |
|---|---|---|---|
| `HUYGENS_SIGN` | `-1` | **고정 상수** | 바비네 뒤집기 부호. 토글 아님 (§1 유도) |
| `HUYGENS_M` | `200` | 상수 | 해석급수 모드 수. 원본 `FLOQUET_M=50`과 분리 |
| `GRID_W_INF` | `360` | 상수 | Tab 0 격자 폭 |
| `GRID_W_FINITE` | **`200`** | 상수 | Tab 1 전용 격자 폭. §7의 절차로 확정 (240 은 기준 기하에서 예산 초과) |
| `USE_Y_SYMMETRY` | `true` | 토글 | `y=0` 대칭으로 절반만 계산. **근사 아님** (on/off 비트 동일) |
| `WIRE_DRAW_EXAGGERATION` | `5` | 토글 | 도선 반지름 표시 과장. `1`이면 실제 크기. 패널 문구의 배율 숫자(`#wireExagVal`)도 이 값에서 채운다 |
| `DRAW_BAND_TITLES` | `true` | 토글 | 캔버스에 글자를 그릴지. `false` 면 **①②③ 밴드 제목과 ① 칸 "입사파 진행 →" 라벨이 모두 꺼져 캔버스에 글자가 하나도 없다.** 화살표 **그림**은 남는다. 게이트 2 판정과 논문 그림 캡처에 쓴다 (아래 참고) |
| `VERIFY_N_ODD` | **`801`** | 상수 | 검증 전용 배열 크기 (**반드시 홀수**). 근거는 §6-1 |
| `DESIGN_ASPECT` | `113/360` | 상수 | 성능 판정 전용 기준 기하. 창 종횡비와 무관하게 항목 10 을 판정한다 |
| `EVANESCENT_EPS` | **`1e-10`** | 상수 | 열별 소멸파 조기종료 임계. **`HUYGENS_M` 보다 먼저 잘라선 안 됨** |
| `T_NS_FIXED` | `64` | 상수 | `T`와 검증 판정 전용 적분 표본 수 (화면 렌더와 별개) |

경사인자는 토글로 만들지 않는다 (§1).

> **논문 그림 캡처 시 `DRAW_BAND_TITLES = false` 를 권장한다.** 캡션을 캔버스에서 패널
> `.hint` 로 옮긴 것과 같은 이유다 — 그림 위에 글자가 얹히지 않는 편이 깔끔하다.
>
> 끄면 **①②③ 밴드 제목과 ① 칸의 "입사파 진행 →" 라벨이 모두 사라져** 캔버스에 글자가
> 하나도 남지 않으므로 그대로 캡처해 쓸 수 있다. 캡처용 토글이므로 **전 밴드에 일관
> 적용한다** — ① 칸 라벨만 남으면 그림이 어색하다.
>
> **진행방향 화살표 그림은 남는다.** 글자가 아니라 파동이 왼→오른으로 진행함을 나타내는
> 그림이라 논문 그림에서도 유지되어야 한다. 화살표까지 없애려면 `drawOverlay()` 의
> `if (band === 0)` 블록 전체를 끄면 되지만, 기본 동작으로 삼지 않았다.
>
> (밴드 순서는 위에서부터 ① 입사파 / ② 차이장 / ③ 전체장으로 고정이고, 패널 `.hint` 에도
> 같은 설명이 있다 — 제목을 꺼도 그림 설명을 캡션에서 가져올 수 있다.)
>
> 같은 토글을 **설계 §10-2 게이트 2** 가 판정에 쓴다. 글자 상자만 예외로 빼는 방식은
> 폰트 메트릭에 의존해 판정이 환경에 좌우되므로 쓰지 않는다 (설계 §15-16).

**`HUYGENS_M = 200` 의 근거.** 막힘 함수가 사각파라 `t_m`이 `1/m`로만 감쇠하고
`g_m ≈ 2πm/d`이므로 **`d`가 클수록 소멸파가 늦게 죽는다.**
최근접 열 `x = 0.80 mm` 에서 완전급수(M=4000, 조기종료 없음) 대비 오차 —
**출하 설정 `EVANESCENT_EPS = 1e-10` 기준:**

| 조건 | M=50 | M=200 | 조기종료 절단 m |
|---|---|---|---|
| λ=12.2 cm, d=10 mm, a=0.5 mm | 1.7e-10 | 1.7e-10 | 42 |
| **λ=1 cm, d=60 mm, a=3 mm** | **5.8e-4** | 1.8e-10 | 249 |
| λ=1 cm, d=30 mm, a=1 mm | 9.1e-6 | 1.4e-10 | 122 |

`d = 60 mm` 에서만 절단이 `m = 249 > 200` 이라 **거기서는 `M` 이 구속 조건이고 `M = 200` 이
실제로 필요하다.** 나머지 둘은 `EPS` 가 먼저 걸려 `M=50` 과 `M=200` 이 같다.
비용 차이는 없다 — Tab 0 렌더는 기본 조건 **5 ms**, 최악 조건(d=60 mm) **22 ms**.

---

## 6. 논문 그림 캡처 시의 환경 값

`nS`가 `Δx/4`에 묶여 있어 **브라우저 창 크기에 따라 달라진다.** 아래는 실측 창 기준이며,
캡처할 때마다 콘솔의 `[검증] 0. 환경 기록`을 그대로 옮겨 적을 것.

```
캔버스 CSS 1196×638 px  ·  aspect = bandH/bandW = 0.1679  (판정 기준 기하 0.3139)
Tab0  gridW=360 gridH=60 Δx=2.9776mm nS=6 Δ'=0.1667mm
Tab1  gridW=200 gridH=40 Δx=5.3598mm nS=6 Δ'=0.1667mm
```

**`aspect` 를 함께 적는 이유.** 항목 10 의 판정은 이 창의 `aspect` 가 아니라
`DESIGN_ASPECT`(0.3139)로 한다. 두 값이 크게 다르면 화면에 보이는 격자와 성능 판정에
쓰인 격자가 다르다는 뜻이므로, 캡처 기록에 둘 다 남긴다.

> **`T_fin_huygens`는 여기 적힌 화면 렌더용 `nS`가 아니라 `nS = 64` 고정을 쓴다.**
> 화면 렌더의 `nS`는 격자 `Δx`가 표현할 수 있는 해상도에 맞춘 값이라 창 크기에 따라 6까지
> 내려가지만, `T`는 논문에 싣는 수치이므로 화면 해상도와 무관하게 고정된 최대 정밀도로
> 계산해야 한다. 즉 **`T` 쪽이 언제나 화면 렌더보다 정확하다.**

### 6-0. 화면 그림의 수치 정확도 (중요)

화면 렌더와 검증은 **서로 다른 `nS`를 쓴다.** 논문 그림의 정확도 근거는 검증 판정값이 아니라
**화면 렌더 쪽**이다.

| | 적분 표본 | 최근접 열 상대편차 |
|---|---|---|
| **화면 렌더** (그림에 보이는 것) | `nS = 6` (창에 따라 변동) | **2.2e-4 ~ 2.2e-3** |
| 검증 판정 | `nS = 64` 고정 | `4.1e-6 ~ 5.9e-6` |

실측(`|Δ| / max|E_해석|`): 화면 렌더 `nS=6`으로 최근접 열에서 단일차수 `3.70e-4`,
다차수 `5.68e-4` (좁은 창에서는 `1.20e-3` / `2.57e-3`까지 올라간다). 같은 점을 `nS=64`로
계산하면 `1.12e-5` / `9.47e-6`.

이 편차는 물리가 아니라 **구적법 오차**다. 최근접 열은 도선 평면에서 `Δx/2`밖에 안 떨어져
있어 커널 `H₁(kr)·(x/r)`의 피크 폭(≈ `x`)이 도선 폭 `2a`보다 좁아진다. `1/nS²`로 줄어든다.
**논문 그림에서 도선 바로 옆 화소를 정량 근거로 쓰지 말 것.** 측정면(`x = 30 mm`) 부근은
문제없다.

### 6-1. `VERIFY_N_ODD = 801` 채택 근거

**401 은 여유가 부족했다.** `401 ↔ 801` 배증 시험은 브라우저에서 `9.17e-4` 로 기준
`1e-3` 대비 **8 %** 뿐이었다 — 창이 달라지면 넘길 수 있는 수준이다. 801 로 올렸다.

`801 ↔ 1601` 배증 시험(같은 영역에서 `|E_1601 − E_801| / max|E_801|`) 실측:

| 조건 | 배증 상대편차 (Node / 브라우저) |
|---|---|
| 단일차수 λ=12.2 cm, d=10 mm, a=0.5 mm | `1.64e-4` / `2.81e-4` |
| 다차수 5개 λ=1.2 cm, d=30 mm, a=1 mm | `1.46e-5` / `2.95e-5` |

여유가 3.6~6.5배다. 대가는 `node huygens/verify.js` 소요 **2.8초 → 6.3초**.

> **⚠ 검증에는 반드시 홀수 `N`을 쓸 것.** 짝수면 도선이 `±d/2`에 놓여
> `e^{−iα_m d/2} = (−1)^m` 때문에 **홀수 차수의 부호가 뒤집힌다.** 다차수 조건에서
> 상대오차 `3.4e-1`로 나타난다.

### 6-2. 대조 영역을 측정면까지로 한정한 근거

`node huygens/verify.js --converge` 실측. `x`는 물리 좌표 고정, `y`는 `T` 측정창과 같은
41 표본(`|y| ≤ 22.5 mm`), `nS = 64` 고정.

```
λ=12.2cm d=10mm a=0.5mm  (d/λ=0.082)
   x [mm] │      N=401      N=801     N=1601
      30 *│    1.19e-3    4.21e-4    1.49e-4
     100  │    3.96e-3    1.40e-3    4.96e-4
     300  │    1.18e-2    4.20e-3    1.49e-3
     540  │    2.10e-2    7.54e-3    2.68e-3

λ=1.2cm  d=30mm a=1mm    (d/λ=2.500, 설계 검증 조건)
   x [mm] │      N=401      N=801     N=1601
      30 *│    1.35e-4    4.79e-5    1.71e-5
     100  │    4.96e-4    1.78e-4    6.48e-5
     300  │    1.12e-3    3.97e-4    1.44e-4
     540  │    2.63e-3    9.38e-4    3.33e-4

λ=1.2cm  d=60mm a=1mm    (d/λ=5.000, Rayleigh 특이점)
   x [mm] │      N=401      N=801     N=1601
      30 *│    5.98e-2    4.23e-2    2.99e-2
     100  │    1.72e-1    1.22e-1    8.64e-2
     300  │    3.84e-1    3.10e-1    2.27e-1
     540  │    1.79e-1    2.62e-1    2.66e-1
```

**결론 — `x`가 커지면 수렴에 필요한 `N`이 비례해 늘어나므로, 대조 영역을 측정면
(`0 < x ≤ 30 mm`)까지로 한정한다.** 같은 `N`에서 오차가 `x`에 거의 비례해 커진다
(`30 → 100 → 300 mm`에서 `1.19e-3 → 3.96e-3 → 1.18e-2`). 영역을 격자 끝까지 넓히면
`Xw = Yw/aspect`가 창 크기에 따라 달라져(실측 287 mm → 540 mm) **판정이 브라우저 창
크기에 좌우된다.** 이 어긋남은 구현 오류가 아니라 유한 배열과 무한 배열의 물리적 차이다.

`d=60 mm`(`d/λ = 5.000`) 줄은 §7의 Rayleigh anomaly를 독립적으로 재확인한 것이다 —
`N`을 4배 키워도 `5.98e-2 → 2.99e-2`에 머물고, `x=540 mm`에서는 오히려 **나빠진다**
(`1.79e-1 → 2.66e-1`).

---

## 7. Rayleigh anomaly — 논문에 쓸 소재

`d/λ`가 **정수**일 때 `κ_m = 0`인 **스치는 차수(grazing order)**가 생긴다. 그 파는 스크린을
따라 진행하므로 **먼 도선의 기여가 거리에 따라 줄지 않는다.**

그래서 **유한 배열이 무한 배열로 수렴하지 않는다.** 실측: `d/λ = 3.000`(λ=1 cm, d=30 mm)에서
상대오차가 `N=101 → 8.3e-2`, `N=401 → 4.2e-2`, `N=1001 → 2.6e-2`, `N=3001 → 1.5e-2`로
머문다. 반면 `d/λ = 2.5`(λ=1.2 cm)는 `N=101`에서 이미 `3.6e-4`로 수렴한다.

그래서 검증 4번의 다차수 조건을 원본 요구서의 λ=1 cm에서 **λ=1.2 cm로 교체**했다.
원래 조건은 `T` 해석식 검증(검증 항목 7)에는 그대로 남는다 — 거기서는 유한 배열이 아니라
해석식만 쓰므로 문제가 없다.

**§6-2의 `d=60 mm`(`d/λ = 5.000`) 줄이 이 현상의 독립 재확인 자료다.**

---

## 8. 성능

**예산: 최악 조건에서 `recompute()` 300 ms 이내.**
최악 조건 = λ=1 cm, d=10 mm, a=3 mm(`= 0.3·d` 상한), N=60, Tab 1.

**판정 기하를 `DESIGN_ASPECT` 로 고정한다.** 창에서 읽은 aspect 로 판정하면 판정이 창
종횡비에 좌우된다 — 넓은 창(aspect 0.167)은 `gridH` 가 40 까지 내려가 셀 수가 절반이라
통과하지만, 같은 코드가 세로로 긴 창에서는 예산을 넘는다. 실제 창 기하의 `recompute()`
값은 **기록**으로 함께 출력한다.

기준 기하(`DESIGN_ASPECT`)에서 `GRID_W_FINITE` 후보별 Node 실측 (프로세스 격리, 7회 중 최소):

| gridW | gridH | nS | 커널평가 | Node (설계 시점) | Node (재측정) | (참고) 환산 ×1.58 |
|---|---|---|---|---|---|---|
| 360 | 113 | 16 | 9.85M | 889 ms | — | 1405 ms |
| 260 | 82 | 12 | 3.84M | 307 ms | — | 485 ms |
| 240 | 75 | 12 | 3.28M | 298 ms | — | 471 ms |
| **200** | **63** | **12** | **2.30M** | **197 ms** | **99 / 100 ms** | **311 ms** |

**`GRID_W_FINITE = 200` 으로 낮췄다** (설계 §9-3 절차 3번). 240 은 기준 기하에서 예산을
넘는다.

### ✅ 2026-08-06 — 브라우저 표시 상태 실측으로 확정

| 기하 | gridW × gridH | nS | 브라우저 | Node (격리·7회 최소) |
|---|---|---|---|---|
| **판정 — 기준 기하 `DESIGN_ASPECT`** | **200 × 63** | **12** | **147 ms** ✅ | **99 / 100 ms** |
| [기록] 실제 창 (캔버스 1196×730, aspect 0.1939) | 200 × 40 | 6 | 100 ms | — |

`147 ms < 300 ms` 로 예산 안이다. 브라우저 계수 실측 `147/99 ≈ 1.48` 로 위 `×1.58` 과
맞는다. **실제 창(100 ms)이 기준 기하(147 ms)보다 빠르다** — 넓은 창은 `gridH` 가 63 → 40
으로 내려가 셀 수가 줄기 때문이고, 판정을 `DESIGN_ASPECT` 로 고정한 이유를 그대로 보여준다.

> **⚠ 위 후보 표의 값을 판정 근거로 쓰지 말 것.** 채택행을 같은 코드·같은 기준 기하로 다시
> 재니 `197 ms → 99~100 ms` 로 절반이 되었다. 코드도 기하도 바뀌지 않았으므로 차이는
> **머신 부하**다. 표는 `gridW` 선택을 위한 **상대 비교용**이고, `×1.58` 환산치도
> **참고값**이다. **판정은 브라우저 표시 상태의 실측으로만 한다.**

### ⚠ 성능을 다시 잴 때 반드시 지킬 것 — 무효가 되는 세 가지 측정

1. **한 Node 프로세스에서 여러 조건을 연달아 재지 말 것.** V8 최적화 해제로 같은 코드가
   3배까지 느려진다(설계 단계 실측 `328 ms → 1206 ms`). `node huygens/verify.js --perf`는
   항목 0·10만 돌리는 격리 모드다. 기본 실행의 항목 10은 **기록이지 판정이 아니다.**
2. **콜드 1회 측정을 쓰지 말 것.** 첫 호출은 JIT 예열 전이라 부풀려진다. Node·브라우저 모두
   7회 중 최소값을 쓴다.
3. **백그라운드 탭에서 잰 값을 쓰지 말 것.** Chrome이 우선순위를 낮춰 3배까지 느려진다
   (실측 201 ms → 553~601 ms, `document.visibilityState === "hidden"`). 앱은 이 경우
   콘솔에 경고를 찍고, **탭이 보이게 되면 항목 0·10을 자동으로 다시 측정한다.**

### 성능 메모 — `hankel1` 의 객체 할당

최악 조건에서 `hankel1`이 **190만 번** 불린다. 객체를 반환하면 Node는 탈출 분석으로 없애
주지만 **Chrome은 그러지 못해 브라우저에서만 3.7배 느려졌다**(브라우저 579 ms vs Node
격리 157 ms). `hankel1Into(x, out)`으로 스크래치 `Float64Array`에 쓰게 바꿔
**579 ms → 201 ms**가 되었다. 결과는 불변이다.

측정한 세 방식(Node, 프로세스 격리, 7회 중 최소):

| 방식 | Node | Chrome |
|---|---|---|
| 객체 반환 | 289 ms | 579 ms |
| **`Float64Array` out (채택)** | **278 ms** | **201 ms** |
| 클로저 변수 | 406 ms | — |

**클로저 변수 방식은 쓰지 말 것** — Node에서 오히려 1.5배 느리다.

**스크래치는 모듈 전역이 아니라 호출자가 넘긴다.** `physics.js` 는 모듈 전역 상태를 두지
않는 것이 원칙이라(§9 파일 경계), `rsDiffAt` 이 `Float64Array(2)` 를 선택 인자 `scratch`
로 받고 `rsDiffGrid`·`T_fin_huygens` 가 하나씩 만들어 넘긴다. 점마다 새 배열을 만들면
Node 가 **249~286 ms → 314~326 ms** 로 25 % 느려진다(실측). 호출자 소유이므로 함수는
여전히 순수하다 — 같은 입력에 같은 출력이고 `scratch` 의 잔여 내용은 결과에 영향이 없다.

---

## 9. 파일 구조와 의존 방향

```
Faraday/
  index.html              ← </body> 직전 <script src="bridge.js"></script> 1줄만 추가 (그 외 무수정)
  script.js · style.css   ← 무수정
  bridge.js               ← 두 앱 공유 · postMessage ↔ DOM 이벤트
  shared/
    floquet-ref.js        ← Floquet 해석식의 유일한 복사본
    floquet-ref.test.js   ← Node 전용
  huygens/
    index.html · style.css
    physics.js            ← 순수 물리. DOM 무접촉
    verify.js             ← 순수 판정. physics.js 에만 의존. DOM 무접촉
    script.js             ← DOM · 렌더 · UI. 판정 로직 없음
```

```
physics.js  ←  verify.js  ←  script.js
                           ↖ floquet-ref.js (정보란의 참고 T 표시용)
```

### ⚠ 스크립트 태그 순서 — 좌우 앱이 의도적으로 다르다

| 파일 | 순서 | 이유 |
|---|---|---|
| `index.html` (원본) | `script.js` → `bridge.js` | 설계 §2-1 이 "기존 `script.js` 태그 **뒤에**"로 못 박았다. 원본 3파일에 허용된 유일한 변경이므로 **옮길 수 없다** |
| `huygens/index.html` (신규) | **`bridge.js`** → `floquet-ref.js` → `physics.js` → `verify.js` → `script.js` | 신규 파일이라 제약이 없다. 아래 결함을 피한다 |

**`bridge.js` 가 먼저여야 하는 이유.** `script.js` 의 IIFE 는 로드 즉시 `resize()` 와
`runVerification()` 을 돈다. `bridge.js` 가 뒤에 있으면 그 시점에 패널이 아직 보여
좁은 iframe 안에서 `#canvasWrap` 폭이 **0** 이 되고, `bandW = cssW − 24 = −24` 라
**aspect·Δx·x 좌표가 전부 음수**가 된다. 실측: `캔버스 CSS 0×298`, `aspect −3.4667`,
`Δx −0.2596 mm`, `최근접 열 x −0.07 mm` → **자가검증 항목 2 가 FAIL** 한다.

**원본도 같은 결함을 갖지만 증상이 없다.** 첫 `resize()` 가 폭 0 을 읽는 것은 동일하다.
다만 원본의 콘솔 검증(J₀·Y₀·T)이 캔버스 기하와 무관해 드러나지 않고, 화면은 `bridge.js`
가 쏘는 `resize` 이벤트로 곧바로 복구된다. 그래서 원본은 고치지 않는다.

**단독 로드 동작은 어느 쪽도 바뀌지 않는다** — `bridge.js` 는 최상위 프레임이면
`if (window.top === window) return;` 로 즉시 반환한다.

**설계 §3은 `huygens/`를 4개 파일로 적었으나 5개로 만들었다.** §10-3(Node 테스트)과
충돌했기 때문이며, 사용자가 파일 수 쪽을 고치기로 결정했다. 경계는 **"순수 물리만"** 한 번에
긋는다 — 렌더까지 나누면 원본 `script.js`와의 구조 대칭이 깨진다.

| 파일 | 담당 | 금지 |
|---|---|---|
| `physics.js` | 베셀·`t_m`·전파차수·`T_inf`, Tab 0 해석급수, Tab 1 RS 적분, `T_fin` | **DOM 접근 0. 모듈 전역 상태 0. 순수 함수만** |
| `script.js` | 상태·DOM 바인딩·렌더, `physics.js` 에 넘길 인자 만들기 | 물리식·판정 로직 |
| `verify.js` | §10-1 의 **1~9 번** 판정 | `physics.js` 외 의존, DOM 접근 |

**`verify.js`는 판정의 단일 출처다.** 브라우저와 Node가 **같은 코드**를 실행한다(이중 가드).
판정을 두 벌 만들면 갈라지므로, `script.js`는 실측 `env`를 넣어 부르기만 하고 판정을 직접
구현하지 않는다.

**항목 0(환경 기록)과 항목 10(`recompute()` 실측)만 `script.js`에 있다.** 둘 다 캔버스 CSS
크기와 `recompute()` 호출에 의존해 브라우저에서만 얻을 수 있기 때문이다. Node 쪽
`--perf` 는 같은 최악 조건의 **물리 계산만** 재는 참고치이고 **항목 10이 아니다.**

**`bridge.js`는 최상위 프레임이면 첫 줄에서 즉시 반환한다** (`if (window.top === window) return;`).
비교 페이지가 iframe 안의 앱을 제어하기 위한 파일이므로, 앱을 단독으로 열었을 때는
리스너도 `?embed=1` 스타일 주입도 없어야 한다. 이것이 **"원본 앱 단독 로드 시 동작·콘솔·
화면이 작업 전과 완전히 동일"** 을 보장하는 근거다.

**`shared/floquet-ref.js`의 첫 줄 주석이 복사본 ↔ 원본 ↔ 설계 문서를 잇는 유일한 추적
고리다.** 두 저장소가 분리되어 있으므로, 원본 `script.js`가 나중에 바뀌면 그 주석의 해시
(`0cf9519`)로 어느 시점의 복사본인지 판별한다.

---

## 10. 실행 방법

로컬 서버가 필요하다. `file://`로 열면 Phase 2의 iframe 통신이 막힌다.

```
npx serve .
http://localhost:3000/           원본 격자 모형
http://localhost:3000/huygens/   하위헌스 대조군
```

브라우저 없이 검증만:

```
node huygens/verify.js            검증 항목 0~10
node huygens/verify.js --perf     항목 0·10 만 (성능은 반드시 이 명령으로 — 프로세스 격리)
node huygens/verify.js --converge 유한 배열 수렴 시험 (§6-2). 판정 아님
node shared/floquet-ref.test.js   §4 표 T(실제, Floquet) 재현
```
